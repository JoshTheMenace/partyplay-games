import { test } from 'node:test';
import assert from 'node:assert/strict';
import { CAMPAIGN_KEY, readCampaign, recordCampaign } from '../src/campaign';
const storage = () => { const data = new Map<string, string>(); return { getItem: (key: string) => data.get(key) ?? null, setItem: (key: string, value: string) => { data.set(key, value); } }; };
test('best stars and scores persist independently for any stage and never regress', () => { const store = storage(); assert.deepEqual(readCampaign(store).stars, Array(10).fill(0)); recordCampaign(9, 3, 800, store); recordCampaign(9, 0, 40, store); const progress = readCampaign(store); assert.equal(progress.scores[9], 800); assert.equal(progress.stars[9], 3); assert.deepEqual(progress.stars.slice(0, 9), Array(9).fill(0)); recordCampaign(0, 1, 280, store); assert.equal(readCampaign(store).stars[0], 1); assert.equal(readCampaign(store).scores[9], 800); });
test('corrupted/mismatched campaign data is recoverable and unsafe values are sanitized', () => { const store = storage(); store.setItem(CAMPAIGN_KEY, '{bad'); assert.equal(readCampaign(store).stars.length, 10); store.setItem(CAMPAIGN_KEY, JSON.stringify({ version: 1, stars: [5, -2, '3'], scores: [-20, 42.4, '4'] })); assert.deepEqual(readCampaign(store).stars.slice(0, 3), [3, 0, 0]); assert.deepEqual(readCampaign(store).scores.slice(0, 3), [0, 42, 0]); });
test('blocked browser storage returns useful progress without crashing gameplay', () => { const store = { getItem() { throw new Error('blocked'); }, setItem() { throw new Error('blocked'); } }; assert.equal(readCampaign(store).saved, false); const result = recordCampaign(0, 2, 550, store); assert.equal(result.saved, false); assert.equal(result.stars[0], 2); });
